# Connection string for the users database. Only the scheme is ever read by the
# analyzers; no value from an environment file enters the graph.
DATABASE_URL=postgresql://users:users@localhost:5432/users
PORT=8080
SENDGRID_API_KEY=
STRIPE_SECRET_KEY=
