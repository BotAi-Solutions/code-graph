# Connection string for the users database. The scheme is what tells the
# analyzer which provider this service talks to.
DATABASE_URL=postgresql://knowledge:knowledge@localhost:5432/knowledge

# Port the HTTP server binds to.
PORT=8080

# Signing key for session tokens. The name is indexed; the value never is.
SESSION_SECRET=replace-me
